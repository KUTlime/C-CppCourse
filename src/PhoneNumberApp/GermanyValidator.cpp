#include "GermanyValidator.h"
#include "InternationalPhonePrefix.h"

// This method can be moved to base class and the validator doesn't 
// need to create this at all.
/*
std::optional<InternationalPhonePrefix> GermanyValidator::CreatePrefix()
{
	return InternationalPhonePrefix(prefix, code);
}
*/

